name: CD

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed
  workflow_dispatch:

concurrency:
  group: cd-production
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    if: >-
      github.event_name == 'workflow_dispatch' ||
      (
        github.event_name == 'workflow_run' &&
        github.event.workflow_run.conclusion == 'success'
      )

    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          # Optional: restrict to a specific port if your server uses non-standard SSH
          # port: ${{ secrets.DEPLOY_SSH_PORT }}
          script: |
            set -e

            echo "🚀 Starting deployment..."

            echo "[1/8] Go to project directory"
            cd ${{ secrets.DEPLOY_PATH }}

            echo "[2/8] Sync latest code"
            git fetch origin main
            git reset --hard origin/main
            git clean -fd

            echo "[3/8] Backup current images"
            docker tag crm-with-blockchain-rag-api:latest crm-api-backup || true
            docker tag crm-with-blockchain-rag-web:latest crm-web-backup || true

            echo "[4/8] Pull latest env from secrets store"
            # If using AWS Secrets Manager:
            # aws secretsmanager get-secret-value --secret-id crm/production/.env \
            #   --query SecretString --output text > .env
            # If using a secrets file already on server, this step is a no-op.

            echo "[5/8] Build & start containers"
            docker compose down
            docker compose up -d --build

            echo "[6/8] Wait for API (via HTTPS + Nginx)"
            SUCCESS=0

            for i in {1..30}; do
              if curl -f --max-time 5 https://bestpurchasestore.com/api/v1/health/live > /dev/null 2>&1; then
                echo "✅ API is healthy"
                SUCCESS=1
                break
              fi
              echo "⏳ Attempt $i/30 failed... retrying in 5s"
              sleep 5
            done

            if [ "$SUCCESS" != "1" ]; then
              echo "❌ Deployment failed! Starting rollback..."

              echo "[ROLLBACK] Stopping containers"
              docker compose down

              echo "[ROLLBACK] Restoring previous images"
              docker tag crm-api-backup crm-with-blockchain-rag-api:latest || true
              docker tag crm-web-backup crm-with-blockchain-rag-web:latest || true

              echo "[ROLLBACK] Restarting previous version"
              docker compose up -d

              echo "[ROLLBACK] Logs"
              docker compose logs --tail=100

              exit 1
            fi

            echo "[7/8] Cleaning unused Docker resources"
            docker system prune -af --volumes || true

            echo "[8/8] Status"
            docker ps
            git log -1 --oneline

            echo "✅ Deployment completed successfully!"
